const DomSelector = {
  CAR_COUNT_INPUT_SELECTOR: "#carInput",
  TRY_COUNT_INPUT_SELECTOR: "#tryInput",
  START_BUTTON_SELECTOR: "#startButton",
  RESULT_VIEW_SELECTOR: "#result",
};

const GameRuleNumber = 4;

export { DomSelector, GameRuleNumber };
